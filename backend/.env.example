PORT=8080
CORS_ORIGIN=http://localhost:3000

# Local Docker MySQL (default)
MYSQL_HOST=localhost
MYSQL_USER=your_user
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=dbms_project
MYSQL_PORT=3306

# Deployment PostgreSQL / Supabase
# DB_DIALECT=postgres
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# SUPABASE_DB_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# PGSSLMODE=require
# PG_POOL_MAX=5

JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh
JWT_ACCESS_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

SYSTEM_ADMIN__EMAIL=admin@example.com
SYSTEM_ADMIN_PASSWORD=ChangeMeAdmin123
SYSTEM_ADMIN_NAME=System Admin
SYSTEM_ADMIN_PHONE=01700000000


BARIKOI_API_KEY=your_barikoi_api_key

MAIL_HOST=smtp.gmail.com
MAIL_PORT=465
MAIL_USER=your_email
MAIL_PASS=your_email_password
MAIL_FROM="your email <your email>"

REDIS_HOST=localhost
REDIS_PORT=6379
# Requires Redis (e.g. docker compose up -d redis). Set false if Redis is not running.
EMAIL_QUEUE_ENABLED=false
EMAIL_QUEUE_CONCURRENCY=2
EMAIL_QUEUE_ATTEMPTS=3
EMAIL_QUEUE_BACKOFF_MS=60000

# Set true on Render when Supabase already has seeds 29–33 applied.
# SKIP_OPERATIONAL_DEMO_SEEDS=true

DEMO_REP_PASSWORD=ChangeMeDemoRep123
DEMO_DISPATCHER_PASSWORD=ChangeMeDispatcher123
DEMO_CITIZEN_PASSWORD=ChangeMeCitizen123
